# Core
psycopg2-binary>=2.9.9
sqlglot>=26.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Database backends (at least one required)
py-pglite>=0.5.0            # Embedded PG via PGlite - no Docker needed (default)
testcontainers[postgres]>=4.4.0  # Real PG via Docker/Podman - full fidelity
